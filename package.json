{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "echo 'No build step needed for single-file HTML project.'",
    "preview": "vite preview",
    "lint": "echo 'Linting not applicable for single-file HTML project.'"
  },
  "dependencies": {
    "vite": "^6.2.0"
  }
}
