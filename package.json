{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp gst212_questions.js dist/gst212_questions.js",
    "preview": "vite preview",
    "lint": "echo 'Linting not applicable for single-file HTML project.'"
  },
  "dependencies": {
    "vite": "^6.2.0"
  }
}
